import React, { useState } from "react";

type Props = {
  isFullScreen: boolean;
  username: string;
};

const Custom: React.FC<Props> = (props) => {
  return (
    <div>
      <Animated />
    </div>
  );
};

export { Custom };

